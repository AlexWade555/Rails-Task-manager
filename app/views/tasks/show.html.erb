<h1><%= @task.title.capitalize%></h1>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<%= link_to 'Go back to all tasks', tasks_path %>
<%= link_to 'Destroy', task_path(@task), data: {method: :delete, confirm: 'Are you sure?'} %>
